name: vaccine domains

on:
  schedule:
    - cron: "0 2 * * *"
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Installing setup-tools
      run: sudo apt-get install python3-setuptools
    - name: Install requirements
      run: pip3 install -r requirements.txt
    - name: Run script
      env: # Or as an environment variable
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}
      run: |
        git checkout -b vaccine-domains-branch
        python3 run_data.py 'vaccine'
        git add ./data/json_files/*/\*.json
        git commit -m 'adding vaccine domain json'
        git checkout master
        git merge vaccine-domains-branch
        git push origin master